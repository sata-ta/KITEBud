# The origin(s) allowed to call this API (comma separated). Used for CORS.
FRONTEND_URL=http://localhost:3000

# Reserved for future database integration (not used by the MVP's in-memory store).
DATABASE_URL=postgresql://user:password@localhost:5432/kitebud
